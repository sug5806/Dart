import 'package:flutter/material.dart';

class StoreDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Container(
      child: new Column(
        children: <Widget>[
          new Container(
//            child: Image.network(src),
            color: Colors.lightBlue,
          ),
          new Text('sdfasdfdsfad'),

        ],
      ),
    );
  }
}
